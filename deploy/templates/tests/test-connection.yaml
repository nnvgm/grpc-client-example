apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grpc-client.fullname" . }}-test-connection"
  labels:
{{ include "grpc-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "grpc-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
